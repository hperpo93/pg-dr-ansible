# ============================================================
# Certificates — generated at runtime by the certs role
# and pushed directly to nodes. Never commit private keys.
# ============================================================
/certs/

# Secrets — copy all.yml.example → all.yml and fill in values
inventory/group_vars/all.yml

# Ansible runtime artifacts
*.retry
.cache/

# Python
__pycache__/
*.pyc
*.pyo

# Ansible Vault (if used to encrypt secrets in group_vars)
# group_vars/all.vault.yml
# vault-password.txt

# Editor / OS
.DS_Store
*.swp
*.swo
*~
.idea/
.vscode/
